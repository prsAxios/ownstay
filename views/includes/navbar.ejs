<nav class="navbar navbar-expand-lg bg-body-light border-bottom sticky-top">
    <div class="container-fluid d-flex justify-content-between align-items-center">
        <a class="navbar-brand d-flex align-items-center text-custom" href="/listings">
            <i class="" style="font-size: 1.5rem; margin-right: 8px;"></i>
            <span class="fw-bold">Ownstay</span>
        </a>

        <div class="mx-auto">
            <form class="d-flex search-form" role="search">
                <input class="form-control me-2 input-custom" type="search" placeholder="Start your search" aria-label="Search">
                <button class="btn btn-search" type="submit"><i class="fa fa-search"></i></button>
            </form>
        </div>

        <ul class="navbar-nav d-flex mb-2 mb-lg-0">
            <li class="nav-item">
                <a class="nav-link text-custom mx-2 nav-link-hover" href="/listings/new">Add New Listing</a>
            </li>
            <% if (!currentUser) { %>
                <li class="nav-item">
                    <a class="nav-link text-custom mx-2 nav-link-hover" href="/login">Login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-custom mx-2 nav-link-hover" href="/signup">Signup</a>
                </li>
            <% } else { %>
                <li class="nav-item">
                    <a class="nav-link text-custom mx-2 nav-link-hover" href="/logout">Logout</a>
                </li>
            <% } %>
        </ul>
    </div>
</nav>

<!-- Custom Styles -->
<style>
    .text-custom, .navbar-brand, .nav-link, .search-form input, .btn-search {
        font-family: 'Inter', sans-serif; /* Font family */
        font-size: 16px; /* Font size */
        color: #333; /* Text color */
    }

    .search-form {
        display: flex;
        align-items: center;
    }

    .search-form input {
        border-radius: 20px 0 0 20px;
        border: 1px solid #ccc;
        padding: 0.5rem 1rem;
        min-width: 250px; /* Adjust width */
        max-width: 100%; /* Ensure responsiveness */
    }

    .btn-search {
        border-radius: 0 20px 20px 0;
        border: 1px solid #ccc;
        background-color: gray;
        color: white;
        min-width: 80px; /* Adjust button width */
    }

    .btn-search:hover {
        background-color: lightgray; /* Hover color */
    }

    .nav-link-hover {
        transition: color 0.3s ease, text-shadow 0.3s ease;
    }

    .nav-link-hover:hover {
        color: #333; /* Hover color */
        text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3); /* Hover effect */
    }

    /* Ensure proper spacing between navbar items */
    .nav-item {
        margin: 0 5px; /* Adjust spacing as needed */
    }
</style>
